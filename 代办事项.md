项目一：Spring Boot + Vue 待办事项应用（JSON 存储）
技术栈严格限定为 Spring Boot + Vue，用 JSON 文件替代数据库，不引入额外框架或插件。
后端：在 src/main/resources 目录下创建 todo.json，初始内容为 []；开发待办增删改查 4 个接口，端口 10011；通过 Java IO 流直接读写 JSON 文件，新增待办自动生成数字 ID。
前端：用 Vue 创建单页面，包含输入框、添加按钮和待办列表；点击列表项切换完成状态，右侧删除按钮移除待办；通过 Axios 调用后端接口，数据实时同步到 JSON 文件。
本地运行：后端直接启动 Spring Boot 主类，前端执行npm run serve后访问localhost:10012，无需额外配置即可操作数据